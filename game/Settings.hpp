#pragma once


namespace ne {

struct Settings {
    //Speed at which notes move
    float scrollSpeed = 1.0f;
    
    //Percentage where the receptors lie
    float judgmentLine = 0.2f;
};

     
} //namespace ne